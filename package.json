{
  "name": "new-folder",
  "private": true,
  "version": "1.1.6",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "sync-android": "powershell -Command \"if (Test-Path android-app/app/src/main/assets/dist) { Remove-Item -Recurse -Force android-app/app/src/main/assets/dist }; New-Item -ItemType Directory -Force -Path android-app/app/src/main/assets/dist | Out-Null; Copy-Item -Recurse -Force dist/* android-app/app/src/main/assets/dist; if (Test-Path C:/Users/Admin/.gemini/antigravity-ide/scratch/assets/dist) { Remove-Item -Recurse -Force C:/Users/Admin/.gemini/antigravity-ide/scratch/assets/dist }; New-Item -ItemType Directory -Force -Path C:/Users/Admin/.gemini/antigravity-ide/scratch/assets/dist | Out-Null; Copy-Item -Recurse -Force dist/* C:/Users/Admin/.gemini/antigravity-ide/scratch/assets/dist\"",
    "build-android": "npm run build && npm run sync-android"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
